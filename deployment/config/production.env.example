# Arke Upload Server - Production Environment Configuration
# Copy this file to production.env and configure for your deployment

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=3000
DEBUG=false

# Worker API URL (where batches are processed)
WORKER_URL=https://ingest.arke.institute

# Upload Directory (inside container)
# The systemd service mounts /data/arke-uploads to this location
UPLOAD_DIR=/tmp/arke-uploads

# Optional: Enable additional logging
# LOG_LEVEL=info
# LOG_FILE=/var/log/arke-upload/server.log
